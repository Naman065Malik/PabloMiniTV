"""Health-check routes.

The `/health` endpoint lives on the root router in `main.py`. This module is
kept for any additional health-related routes (e.g. `/health/ready`,
`/health/live`) that may be added later.
"""

from __future__ import annotations
